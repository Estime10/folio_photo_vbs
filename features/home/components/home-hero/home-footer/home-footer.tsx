/**
 * Contenu du notch (rendu par BlurCard dans la zone encoche).
 * La zone a déjà la bonne taille (50% × 45%) et le padding, il suffit de remplir.
 */
export function HomeFooter() {
  return (
    <div className="h-full w-full rounded-lg bg-yellow-400" aria-hidden />
  );
}
